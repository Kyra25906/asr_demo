# asr_demo 项目约定

## 每轮收尾硬性检查（代码写完 → 做完以下 7 步才算本轮结束）

```
□ 1. 全量测试通过（..\.venv\Scripts\python.exe -B -m unittest discover）
□ 2. 更新 PROJECT_TASK_CHECKLIST.md（任务状态 + 维护日志 + 测试基线）
□ 3. 追加 LEARNING_REVIEW_FROM_DEVELOPMENT.md（本轮知识）
□ 4. 更新 NEXT_SESSION_HANDOFF_2026-08-9.md（最近真实验收 + 基线）
□ 5. 用完整 7 段教学格式做总结，大白话+专业词语的形式（目的/技术路线/设计原因/实现/知识/验收/下一步）
□ 6. 体验走查（UX Walkthrough）：本轮改动有"用户能看到的输出或交互路径"变化时必做，
   按 docs/UX_WALKTHROUGH_CHECKLIST.md 九维表走查并留证据；纯内部逻辑轮次注明
   "无输出面变化"跳过。注意：功能验收通过（REAL_OK）≠ 体验通过（UX_CONFIRMED），
   体验的最终裁决权在用户，agent 不得代填。
□ 7. 在进入下一步前，用大白话解释一下下一步为什么这样设计
```

以上 7 步和写代码、跑测试一样属于"本轮工作"，不是"做完后再想要不要做"。

## 必须维护的三份文档

每轮开发或每天结束工作时，**必须**更新以下文件：

| 文档 | 路径 | 更新时机 |
|---|---|---|
| **任务清单** | `docs/PROJECT_TASK_CHECKLIST.md` | 每轮状态变更时 |
| **学习日志** | `LEARNING_REVIEW_FROM_DEVELOPMENT.md` | 每轮结束后追加 |
| **交接文档** | `docs/NEXT_SESSION_HANDOFF_2026-08-09.md` | 功能闭环完成或当天工作结束时 |

注意：这是可执行要求，不是建议。不要只在做完后才想"要不要更新"——更新这三份文档本身就是每轮工作的一部分。

## 用户背景

低年级本科生。需要按时完成项目，也希望理解软件工程知识。讲清模块职责、数据流、设计取舍、失败边界和验收方法。

## 每轮回答格式

1. **目的** — 解决什么真实问题，完成后获得什么能力
2. **技术路线** — 数据流、涉及模块
3. **设计原因** — 为什么这样拆分、替代方案、取舍
4. **实现功能** — 新增/修改的文件、类/函数职责
5. **本轮知识** — 可学到的 Python、架构、测试或工程知识
   - 用户要求（2026-08-14 记录）：每个知识点按**三段式**展开，目的是积累开发经验：
     1. **白话解释**：用生活化比喻或大白话说明这个知识是什么、解决什么问题；
     2. **专业术语**：给出正式名称/概念（含英文术语），让知识"叫得上名"；
     3. **项目实际体现**：指出本项目哪个文件/哪段代码/哪个决策用到了它，让知识"指得到代码"。
   - 用户补充要求（2026-08-14 记录）：**面向零基础开发者**，讲解要更细致——
     - 不跳步：每一步推理都展开，不省略"显然"的环节，不假设已掌握前置知识；
     - 先解释前置概念：出现专业词先解释它本身（如讲"指数退避"前先解释"重试"）；
     - 拆最小知识块：一个知识点含多个子概念时，先逐个讲清再合起来；
     - 给反面例子：说明"不知道这个会怎样"，帮助理解知识的价值；
     - 讲完留确认点：问一句"这里有没有不清楚的"，不默认对方听懂了。
6. **验收方法** — 准确命令、预期输出、正常/边界/失败路径
7. **下一步** — 验收通过后做什么、本轮故意不做什么

每轮结束用简短列表总结：新增能力、已验证内容、尚未包含内容、下一轮目标。

## 工作透明原则（用户要求，2026-08-14 记录）

用户要求：**每一步操作，无论大小，都要讲清楚"干了什么、为什么这么干、影响什么"，让用户能把握全程工作**，而不是只看到最终结果。这是可执行要求，不是建议。

- 每次工具操作（读文件、改代码、跑测试、提交、文档改动）都用一两句大白话说明目的和预期影响，先说明再做；
- 说明要具体到"哪个文件哪一段、改了什么、为什么改"，不能只报"已修改"；
- 不静默连续执行一串操作；关键决策（设计取舍、改动范围、方案选择）先讲理由再动手；
- 复杂步骤拆小，每步留一个用户能看懂、能提问的停靠点；
- 7 段总结仍保留，但透明是逐步的，不只是轮末一次。

**推进节奏（用户要求，2026-08-14 记录）**：每完成一个可独立验证的环节就**停下**，把成果、影响和当前状态讲清楚，等用户确认或提问后再继续；不一口气推完整轮、不连续执行一串用户来不及看的操作。用户说"继续"才继续。

**对话表达规范（用户要求，2026-08-14 记录）**：
- **分层清晰**：回复按"结论 → 理由/细节 → 下一步"组织，用短标题或列表分段，一段只讲一件事；不把多个话题堆在一大段里。
- **术语必须解释**：出现专业词或英文术语时，紧跟一句大白话（"它叫什么、解决什么问题、本项目用在哪"）；宁可多说一句，不假设用户已懂。
- **不催用户**：减少"要继续吗""认可吗"这类追问频率。只在真正需要用户决策（方案选择、授权、方向取舍）时才问，且一次最多问一个核心问题；其余情况讲清楚后自然停住，由用户主动决定何时继续或提问。

**删除/重构类轮次必须写迁移对照（用户要求，2026-08-14 记录，2026-08-14 补充质量维度）**：删除或重构旧路代码时，必须写清楚"被删/被替换的旧路做了什么 → 新路如何获得该功能"，登记在 `docs/PROJECT_ARCHITECTURE.md` 5.3 职责迁移对照表，并**逐项标注用户可见输出质量状态（等价/降级/丢失，见 5.4）**——防止"代码删了、能力也丢了"，更要防止"动作迁移了、但用户看到的体验质量退化了"（后者仅靠自动测试发现不了，agent 必须对照旧链话术逐项核验，不得自行判定"功能等价"）。

## 开发原则

- 每轮只推进一个可独立验证的能力
- 先定义稳定数据结构和接口，再连接真实服务
- 先单元测试，再集成测试，最后真实验收
- 外部服务失败不能破坏已完成的核心工作
- 原始数据优先保存，模型推断不覆盖原始事实
- 主流程保持精简，复杂业务放独立模块
- 模块间传递数据对象或明确接口，不直接控制彼此的底层资源

## 代码设计

- 优先清晰小模块和显式依赖
- 依赖注入，业务类不内部创建重型服务
- 区分生命周期：程序级 / 会话级 / 单次请求级
- 入口文件只负责创建对象、连接依赖、控制主循环、管理状态
- 入口文件不拼接提示词、解析外部 JSON、实现存储协议、包含大量业务规则
- 配置集中管理，密钥从环境变量或 .env 读取，不硬编码

## 测试要求

分层推进：单元测试(Fake) → 集成测试(Fake连线) → 真实服务测试(固定输入) → 端到端(最后才接麦克风/模型/文件)
- Arrange–Act–Assert，每个测试只验证一个行为
- 同时覆盖正常、边界和失败路径
- 测试公共接口，不依赖私有实现
- 修复后必须运行旧测试防回归
- 文件测试用临时目录，不污染真实数据

## 真实验收

修改触及麦克风、真实 ASR、真实 LLM、持久化或主流程时，自动测试通过后必须主动安排真实验收。
验收前说明：真实输入、数据外发范围、观察指标、成功/失败标准。
验收后用终端输出、记录数量、文件内容等真实证据更新状态。
真实数据外发必须取得明确授权，历史单次授权不能自动扩大。

**功能验收与体验验收分离（用户要求，2026-08-14 记录）**：功能验收（REAL_OK）证明
"系统行为符合规格"，体验验收（UX_CONFIRMED）证明"真实使用者能看懂、不困惑、节奏顺"。
agent 只负责收集体验证据（按九维走查表逐项记录输出流证据），体验最终裁决权在用户；
agent 的"验收通过"表述必须区分是功能还是体验，不得混写。详见
`docs/UX_WALKTHROUGH_CHECKLIST.md`。

## 项目关键路径

- 真实项目：`C:\Users\dahli\Desktop\asr_demo`
- 正式解释器：`.venv` 中的 Python 3.11.9
- 测试命令：`.\.venv\Scripts\python.exe -B -m unittest discover`
- 工作分支：`codex/asr-demo-unified-understanding`（推送到 total remote）
- 数据边界：`.env`、`audio/recordings/`、`results/`、`.venv*` 不提交
