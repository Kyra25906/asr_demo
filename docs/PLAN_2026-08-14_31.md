# asr_demo 比赛冲刺计划 2026-08-14 ~ 08-31（18 天，周末不休）

**目标**：8 月 31 日交出比赛完整 Demo（语音识别 → 结构化 → 追问确认 → 结束 → 导出记录 → 语音播报）。
前端/Live2D 由 C 负责，不占本计划。

**现状（8.13）**：全量 428 项测试通过；主链（识别/结构化/追问）已真实验收；缺上下文连续性、结束导出、消息链路、TTS；main 还留双轨代码（flag + 旧 submit + 三道旧命令门卫）。

## 任务顺序（Demo 为主线，先清债再盖楼）

| # | 日期 | 天数 | 任务 | 目标 |
|---|---|---|---|---|
| 1 | 8.14–8.15 | 2 | 补上下文 `MAIN-SESSION-CONTEXT-01` | 第二段能看到第一段，结束计数正确 |
| 2 | 8.16 | 1 | 运行时加固 `MAIN-RUNTIME-HARDEN-01` | 修正实验段计数、唤醒退避、删重复分支 |
| 3 | 8.17–8.20 | 4 | 清理双轨 `CLEANUP-FLAGS/SUBMIT/COMMAND/NAMING/VERIFY` | 删 flag 和旧 submit、统一命令入口、去影子命名、真实验收 |
| 4 | 8.21–8.24 | 4 | 结束+导出 `LLM-10`+`SESSION-02`+`EXPORT-01` | 结束总结、按 session 聚合、导出 Markdown/JSON |
| 5 | 8.25–8.27 | 3 | 消息链路 `PRESENT-INTEGRATE-01` | 追问/回执有条理走到屏幕和 TTS，不再散落 print |
| 6 | 8.28–8.30 | 3 | TTS 半双工 `TTS-01`→`TTS-05` | 系统语音播报追问和回执，失败不破坏录音 |
| 7 | 8.31 | 1 | Demo 联调 | 2~3 分钟脚本连续跑 3 次 |

## 可并行填缝（P1 纯类型合同，不占主线）

清理步骤之间有空档时抽空铺，只定义类型和扩展点、不改 main 行为（feature flag 默认 false），8/31 后接真实能力时能少等一天：

```text
QUERY-TYPES-01 + SAFETY-TYPES-01 + KNOWLEDGE-PROTOCOLS-01 （并行，纯新文件）
→ UNIFIED-QUERY-01 （统一理解加 QUERY 分支）
→ DISPATCH-QUERY-01 （分派加 KNOWLEDGE_BASE 目标）
→ BYPASS-QUERY-01 + CONFIG-QUERY-SAFETY-01 + RAG-CONTEXT-CONTRACT-01
→ 三项测试 + 现有测试数据更新
→ 全量 ~444 项通过
```

> 规则：不打断 Demo 主线；每次仍只推进一个可独立验证能力；真实能力（SAFETY-INTEGRATE-01、RAG-CONTEXT-01、QUERY-ANSWER-01）仍在 PRESENT 稳定后再展开，多为 8/31 之后。

## 推迟到 8/31 之后

- 真实 RAG、真实安全规则引擎、设备查询 HTTP 接入
- TTS 全双工、GPT-SoVITS 音色、Word/PDF 美化导出
- 前端/Live2D（C 负责，随 C 的排期）

## 提醒

- 每个任务仍是「一个可独立验证的能力」，每轮收尾：全量测试 → 更新三份文档 → 7 段总结。
- 第 3 步 `CLEANUP-VERIFY`、第 4 步导出、第 6 步 TTS 都要真实口述验收，不能只看测试绿。
- 第 1 步前必须先补上下文，否则多段演示断链，后面全乱。
