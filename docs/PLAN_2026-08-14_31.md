# asr_demo 比赛计划 2026-08-14 ~ 09-15（两阶段）

**阶段一（8/31）**：线上评审提交版——**完整功能 Demo**（识别 → 结构化 → 追问确认 → 结束 → 导出记录 → 消息链路 → TTS 语音播报 + 轻量前端），配评审视频/设计文档/材料。
**阶段二（9 月十几号路演）**：现场演示打磨（TTS 完善、PRESENT 时机、联调 3 次、与 C 前端/Live2D 对接）。
前端/Live2D 主体由 C 负责；本计划含**轻量前端（最多 1 天）**作为评审演示用。

**评审"相对可控"的含义**（用户 8.14 澄清）：演示输入由我们掌控——用标准说法/标准词即可跑通，不会被"说非标准表达"或"ASR 识别不准"刁难。**功能不砍**，标准脚本一次跑通即可。

**现状（8.14 夜）**：清理五连已完成四刀（FLAGS/SUBMIT/COMMAND/NAMING 均 REAL_OK/AUTO_OK，A+B 兜底/显示一致性已闭环），468 项测试通过；GAPS-01/02 已逐项决策并登记（固定结束语/DEFER/回答编号提示待实施）。**当晚评委审计新增三笔此前漏记的债**：`RESTORE-NONBLOCK-01`（P0，非阻塞录音能力丢失且 main 仍打印"无需等待"）、`RESTORE-DEGRADED-HINT-01`（P1，LLM 降级无人话）、`SYNC-UI-CLAIMS-01`（P1，2 句谎话 + 2 处误导）；九维体验走查已完成（11 项 UX 问题 UX-01~11，体验状态 UX_ISSUES）。**推进闸门改为"硬问题清零即进 PRESENT，不设固定日期"**（软问题与 PRESENT 并行、出现即修）。缺导出、PRESENT、TTS、轻量前端。

## 阶段一任务顺序（8.14–8.31，先清债再盖楼；硬问题清零即进 PRESENT，不设固定日期）

> **推进闸门（用户 2026-08-14 决策）**：硬问题（崩溃 / 数据丢错 / 核心交互失败，判据见任务清单第 1 节）修完即进 PRESENT，**不设固定日期**（进展快则提前）；软问题（显示 / 话术 / 误识别但语义兜底）与 PRESENT 并行、出现即修。下表只表达"先做什么后做什么"的顺序，不再做日历承诺。
>
> 独立任务清单（按任务逐条列出、含现状/闸门规则/任务表）见 `PHASE1_TASK_LIST_2026-08-15_31.md`。

| # | 阶段 | 任务 | 目标 |
|---|---|---|---|
| 0 | **硬债闸门**（最先） | `RESTORE-NONBLOCK-01` 恢复非阻塞录音（P0）+ `GAPS-FIX-END-01` 结束语追问确认 + `GAPS-FIX-DEFER-01` 按编号暂缓 + `ANSWER-FALLBACK-ADJACENCY-01` 无编号兜底"紧邻"约束 | 连续口述不卡、结束语不崩、暂缓生效、回答不跨段误判；拆掉"无需等待 / 旧流程继续"两句谎话 |
| 1 | 清理收尾（闸门内） | NAMING 轻量复验 + `GAPS-REVERIFY-01` 复验（⑤②现状复查 + ③PHG/E 复测） | 非标准输入不崩、体验缺口闭合 |
| 2 | 总验收（闸门内） | `INTENT-02-CLEANUP-VERIFY-01` 清理后总验收 | 五类口述连续会话，清理 + GAPS 定稿，真实功能不减 |
| 3 | 闸门后 | 结束总结 + 导出 `LLM-10` + `SESSION-02` + `EXPORT-01` | 每次实验可导出 Markdown/JSON 记录（评审材料的数据证据） |
| 4 | 闸门后 | 消息链路 `PRESENT-INTEGRATE-01`（基础版） | 追问/回执/确认消息有条理输出到屏幕与消息流，不再散落 print |
| 5 | 闸门后 | TTS 半双工 `TTS-01`→`TTS-05` | 系统语音播报追问和回执；失败回退终端文本、不破坏录音；打断策略半双工第一版 |
| 6 | 闸门后 | 轻量前端（≤1 天） | 简单页面实时显示对话消息（喂 PRESENT 消息流）；与 C 正式前端不冲突 |
| 7 | 闸门后 | 联调 + 评审材料 | 2~3 分钟标准脚本连续跑 3 次；录评审视频；整理设计文档与相关材料；提交包 |
| 并行 | 空档（8.15 起） | 软问题：`RESTORE-DEGRADED-HINT-01` 降级人话 + `SYNC-UI-CLAIMS-01` 文案修正 + `GAPS-FIX-ANSWER-HINT-01` 编号提示 + UX 系列；铺路：Phase 1a/1b（三组类型合同，不占主线） | 软问题出现即修不阻塞；QUERY/SAFETY/KNOWLEDGE 类型与 Fake 就位，feature flag 全关行为不变 |

## 阶段二任务顺序（9/1–9/15，路演前）

| # | 日期 | 任务 | 目标 |
|---|---|---|---|
| 8 | 9/1–9/4 | TTS 完善：分句播放、打断、唤醒提示替换"我在，请说" | 现场语音体验完整 |
| 9 | 9/5–9/8 | PRESENT 完整：输出预算、编号分离、时机（不打断用户说话） | 输出有条理、不混乱 |
| 10 | 9/9–9/12 | Demo 联调：脚本打磨 + 连续 3 次；与 C 前端/Live2D 对接 | 路演一次成功 |
| 11 | 9/13–9/15 | 路演预演 + 备选方案（断网降级、TTS 失败回退） | 现场从容 |

## 评审材料清单（用户 8.14："视频/设计文档/相关材料"，材料未定义）

- **评审视频**：2~3 分钟标准脚本录屏（含语音播报、屏幕消息、导出记录展示）；
- **设计文档**：基于 `PROJECT_ARCHITECTURE.md` 整理评审友好版（模块职责/数据流/验收证据）；
- **相关材料（未定义，建议候选）**：README、测试基线报告（468 项）、真实会话样例数据导出、TTS/前端说明、数据边界说明——具体按评审通知补。

## 提醒

- 每个任务仍是「一个可独立验证的能力」，每轮收尾：全量测试 → 更新三份文档 → 7 段总结。
- 闸门内（第 0/1/2 步）与闸门后的第 3/5/6 步涉及主流程/新模块，都要真实口述验收，不能只看测试绿。
- 硬问题清零是进 PRESENT 的唯一闸门：真实验收暴露问题先按"硬/软问题"判据归类（见任务清单第 1 节），不得"发现即必修"。
- 标准演示脚本（供评审/路演）：唤醒"小科小科" → 实验口述 ×2-3 → 追问 → 回答（带编号）→ 查看 → 结束实验记录 → 展示导出记录；全程标准说法（演示可控，不考验非标准输入）。
