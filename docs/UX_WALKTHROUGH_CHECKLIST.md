# 体验走查清单（UX Walkthrough Checklist）

首版：2026-08-14（用户提出"终端输出看不出用户体验如何"后建立）

> 本文档定义"体验验收"的固定检查表、标准走查脚本、记录模板和判定规则。
> **功能验收与体验验收是两个独立维度**：
> 功能验收（测试绿、REAL_OK）证明"系统行为符合规格"；
> 体验验收（UX 走查）证明"真实使用者能看懂、不困惑、节奏顺、不被打扰"。
> 二者不能互相替代：`REAL_OK` 不等于"体验好"。
>
> 与 `OUTPUT_PRESENTATION_POLICY.md` 的关系：该文档定义输出职责与政策
> （应该输出什么、什么渠道、什么顺序）；本文档定义如何验收输出
> （实际输出是否达到政策要求、体验是否可接受）。PRESENT 落地后，
> 用本文档第 8 节的基线逐项对比，证明体验改善而非只报"功能已集成"。

## 1. 为什么需要（背景与根因）

用户 2026-08-14 提出：每轮 agent 验收只聚焦"能力有没有实现"，忽略"用户体验如何"；
终端输出难以看出体验好坏，双方对"最终构建的东西怎么样"说不清楚。

根因：

1. **证据类型错配**：功能验收证据（测试数、JSONL 数、session_id、token 数）
   证明"功能正确发生了"，不证明"体验好"。
2. **体验是人的判断**：体验 = 真实使用者的主观感受 + 可观察行为，
   agent 无法替真实使用者裁决，只能收集证据。
3. **输出本身未分层**：`main.py` 42 处散落 `print()`，用户对话、识别结果、
   开发调试信息（`[统一链] 目标=权限=缺失字段=…`）、状态混在同一终端，
   实时看不过来（2026-08-14 基线会话中用户直接反馈"边口述边判断看不过来"）。

## 2. 适用范围（何时必须走查）

- **必须走查**：本轮改动改变了"用户能看到的输出或交互路径"——主流程输出、
  屏幕/TTS 消息、交互顺序、错误提示、前端渲染、唤醒/结束路径。
- **可不走查**：纯内部逻辑（无输出面变化）的轮次，功能验收即可，
  但须在交接文档说明"本轮无输出面变化"。

判断方法：问一句"用户运行程序时，屏幕上/耳朵里看到听到的内容，
跟上一轮比有没有变化？"——有变化就必走查。

## 3. 九维走查表

每维统一格式：**检查方式（操作 → 预期 → 失败标志）→ 证据来源**。
证据来源分三类：`代码静态`（读代码可判定）、`输出流`（看会话输出文件可判定）、
`用户裁决`（必须真实使用者表态）。

| # | 维度 | 检查方式 | 失败标志 | 证据来源 |
|---|------|---------|---------|---------|
| 1 | 信息分诊（Output Channeling） | 全程看屏幕：除对话/回执/状态/待确认列表外，是否出现 `目标=`、`权限=`、`缺失字段`、request_id、文件路径、异常堆栈等开发语言 | 出现上述任一调试语言 | 输出流 |
| 2 | 顺序正确（Turn-taking） | 用户上一句的回答，其回执必须先于任何新消息出现；追问不得早于回执 | 先出现新追问/新状态，后出现回执 | 输出流 |
| 3 | 问题唯一（One Question at a Time） | 每个安全间隙内，用户明确知道"当前要我回答的一个问题"；待确认列表只作参考，不要求立即回答 | 同时出现两个无主次的问题；或系统在等待回答但屏幕没有明确问题文本 | 输出流 + 用户裁决 |
| 4 | 编号一致（Numbering Consistency） | 屏幕上的实验步骤编号、问题编号与内部 utterance 编号分离；用户编号不跳号、不混淆 | 显示"第4段"而实验步骤只有3个；确认答复占用了步骤编号 | 输出流 |
| 5 | 反馈及时（Feedback Latency） | 每段口述后，"已记录/已确认/降级保存"等状态在进入下一轮监听前可见 | 口述后长时间无任何反馈，且无"处理中"说明 | 输出流 |
| 6 | 容错可见（Fail-soft Visibility） | 降级/保存失败/识别失败时，用户知道"发生了什么 + 数据是否已保存 + 下一步怎么做" | 只有异常类名/堆栈，无面向用户的人话说明 | 输出流 |
| 7 | 可理解性（Plain Language） | 屏幕与 TTS 文本无 JSON 字段名、token 数、模型名、文件路径、英文错误码 | 出现上述任一 | 输出流 |
| 8 | 语音预算（Voice Budget） | TTS 一次最多一个追问+一个回执；不朗读"已记录"；不打断用户说话（TTS 未接前此维记"不适用/待TTS轮"） | 朗读长段、打断用户（TTS 轮才可判定） | 用户裁决（TTS 轮） |
| 9 | 响应速度（Response Latency） | 从说完整句到屏幕出现反馈的耗时：识别完成到反馈的间隔，记录实测值；不出现"无响应且无提示" | 口述后超 2 秒无任何新内容且无"处理中"提示（LLM 处理期须有说明） | 输出流（时间戳） |

## 4. 标准走查脚本（固定路径）

场景：模拟评审/路演的连续会话。**执行原则：口述者只看脚本提示，不看屏幕；
屏幕输出由程序重定向到文件，判断在事后离线进行。**

```text
第 0 步  启动程序，输出重定向到 results/walkthrough_baseline_<日期>.txt
         （冷启动约 113 秒；看到"实验语音智能体已启动"后进入第 1 步）
第 1 步  唤醒："小科小科"        → 记录唤醒反馈（文字+提示音）
第 2 步  实验口述 1："先加入5毫升缓冲液"
第 3 步  实验口述 2（依赖1的内容）："加热到六十摄氏度"
第 4 步  若产生追问，回答（带编号）："问题一，是5毫升"
第 5 步  查看："查看待确认问题"
第 6 步  结束："结束实验记录"    → 记录结束汇总输出
第 7 步  会话结束后，逐项核对九维表，填写记录模板
```

每一步的记录内容：**屏幕原文（照抄）、时间点、口述者此刻是否知道下一步做什么**。
"口述者是否知道下一步"这一栏由口述者事后口头回答，agent 不得代填。

## 5. 记录模板

```text
走查日期：            会话 ID：
口述者：              走查记录人（agent）：

| 步 | 用户输入（原文） | 屏幕输出（原文照抄） | 耗时 | 九维命中/失败 | 口述者知道下一步吗 |
|---|-----------------|---------------------|------|--------------|------------------|
| 1 | 小科小科         |                     |      |              |                  |
| 2 | ...             |                     |      |              |                  |

九维逐项结论：
| 维度 | 结论(✓/✗/不适用) | 证据（引用上表输出原文或行号） |
|------|-----------------|-------------------------------|
| 1 信息分诊 |                 |                               |
| 2 顺序正确 |                 |                               |
| 3 问题唯一 |                 |                               |
| 4 编号一致 |                 |                               |
| 5 反馈及时 |                 |                               |
| 6 容错可见 |                 |                               |
| 7 可理解性 |                 |                               |
| 8 语音预算 |                 |                               |
| 9 响应速度 |                 |                               |

总体结论：UX_状态（见第 6 节）+ 一句话体验描述
```

## 6. 判定规则与状态

状态定义（写进任务清单）：

| 状态 | 含义 |
|---|---|
| `UX_PENDING` | 本轮涉及输出面但尚未走查 |
| `UX_WALKED` | 走查完成，证据记录在本文档第 8 节 |
| `UX_CONFIRMED` | 用户实际体验后确认通过 |
| `UX_ISSUES` | 走查发现体验问题（登记任务清单：现象/复现路径/期望） |

流转：`UX_PENDING → UX_WALKED → UX_CONFIRMED 或 UX_ISSUES`

分工边界（必须遵守）：

- **agent 负责**：执行走查、逐行读输出文件、收集证据、判定可客观判定的维度
  （1/2/4/5/6/7/9 及 3 的输出流部分）。
- **用户负责**：最终裁决"是否顺、是否困惑"（3/8 的主观部分 + 整体结论）。
- **禁止**：agent 把"功能验收通过"写成"体验验收通过"；
  `UX_WALKED` 不能替代 `UX_CONFIRMED`。

## 7. 与现有流程衔接

1. **每轮收尾检查（CLAUDE.md 第 6 步）**：真实验收后加子步"体验走查
   （本轮改动有输出面变化时必做，纯内部轮次注明'无输出面变化'跳过）"。
2. **任务清单**：状态定义增加 UX 列；涉及输出面的任务，完成标准加
   "体验状态达到 UX_WALKED 及以上"。
3. **基线对比**：本文档第 8 节是首份基线；将来 PRESENT/TTS/轻量前端每完成一步，
   建议用同表复走、逐项对比 ✗→✓、写进交接文档（绑定与否由用户按轮次决定；
   2026-08-14 暂不与 PRESENT 强制绑定）。
4. **证据升级**：8/29 轻量前端、8/30 评审视频阶段，证据从"终端文字"
   升级为"录屏 + 消息流时间戳"，九维表直接复用。

### 7.5 UX 价值追踪表（UX 问题 ↔ 解决任务映射，2026-08-14 建立）

> 用途：回答"当前阶段的工作对体验有没有用"。每轮任务完成后，对照本表
> 说明解决了哪些 UX 项；某 UX 项闭环时，复走九维表对应维度并记录 ✗→✓。
> 这不是"绑定 PRESENT"，是"每项体验问题都有明确归属任务"。

| UX 问题 | 由谁解决 | 阶段 |
|---|---|---|
| UX-03 无编号回答被当实验段（显示层） | `GAPS-FIX-ANSWER-HINT-01` 回答编号提示 | GAPS（当前） |
| UX-05 结束语识别失败无提示 | `GAPS-FIX-END-01` 固定结束语提示 | GAPS（当前） |
| UX-09 事件提示音 | `UX-FIX-TONE-01`（feedback 事件音 + 消息链路触发） | PRESENT 后 |
| UX-01 DEBUG 混排 / UX-02 追问被淹没 / UX-04 降级无人话 / UX-06 双句号 / UX-07 编号未分离 / UX-08 汇总开发语言 | `PRESENT-INTEGRATE-01` 消息链路 | PRESENT（硬问题修完即进，不设固定日期） |
| UX-11 用户版/管理员版输出分层 | `UX-MODE-01` = PRESENT-INTEGRATE-01 用户可见验收标准 | PRESENT（同上） |
| UX-10 嘈杂识别 | ASR 任务线（语料段 29-31 已就绪，可随时评测） | ASR 线（与 PRESENT 并行） |

## 8. 走查记录（基线）

> 基线走查日期：2026-08-14。程序：`src/main.py`（PRESENT 未集成，
> 输出仍为 42 处散落 print）。
> 原始输出证据：`results/walkthrough_baseline_session_20260814_174441.txt`
> （用户本机终端实录，会话 20260814_174441，口述者为用户本人）。
> **基线意义**：记录 PRESENT 之前"乱在哪、缺什么"，供 PRESENT/TTS/前端
> 各轮用同表复走逐项对比。

### 8.1 逐段记录

| 步 | 口述脚本 | ASR 实际识别 | 屏幕关键输出（摘录） |
|---|---|---|---|
| 唤醒 | 小科小科 | 小科小科 | 唤醒成功：小科小科 + `状态变化：idle -> session_active`（调试） |
| 1 | 先加入5毫升缓冲液 | 先加入防生缓冲液（误识别） | `[统一链] 第1段：…已创建待确认问题 1：您说的'防生缓冲液'是指'磷酸缓冲液'还是'仿生缓冲液'？`（追问埋在开发行内） |
| 2 | （补说）先加入5毫升缓冲液 | 先加入5毫升缓冲液 | 记为实验段，无编号回答未匹配（用户可能以为已回答问题1） |
| 3 | 加热到六十摄氏度 | 加热到60摄氏度 | 记为实验段 |
| 4 | （无口述/环境音） | 제가. | 降级 degraded_note，**无面向用户的人话提示** |
| 5 | （无口述/环境音） | .Good. | abstention，无用户可读反馈 |
| 6 | 问题一是指加热缓冲液 | 问题一是指加热缓冲液 | `已将对问题 1 的答复的实体字段 ['action','object'] 填入。 仍需确认。。`（开发语言+双句号） |
| 7 | 查看待确认问题 | 查看待确认问题 | `当前共有 1 个待确认问题：- 问题 1（待回答）：…` **友好 ✓** |
| 8 | 结束实验记录（第1次） | .别束实验记录。（误识别） | 无任何"未识别到结束指令"提示，用户不知没结束 |
| 9 | 结束实验记录（第2次） | 结束实验记录 | `检测到结束指令，停止接收新的实验口述。` + 结束汇总 + 待确认列表 |

### 8.2 九维结论

| # | 维度 | 结论 | 证据 |
|---|------|------|------|
| 1 | 信息分诊 | **✗** | `[LLM请求]/[LLM响应]/token usage`、录音文件路径、`rtf_avg` 进度条、`状态变化` 全部打在同一屏幕，与用户信息混排 |
| 2 | 顺序正确 | ✓ | 全程未出现"先新追问后回执"错序；但回执本身难懂（可读性归维度 7） |
| 3 | 问题唯一 | **✗** | 追问文本埋在 `[统一链]…` 开发行内，无"当前问题区"；第 2 段用户无编号回答被当实验段——用户不知道"此刻要我回答什么" |
| 4 | 编号一致 | **✗（部分）** | "问题 1"编号前后一致 ✓；但"第 N 段"=utterance 编号，未与"实验步骤 N"分离（违反 POLICY 第 5 节），段 4/5 降级后"提交 4 段"让用户难以对应 |
| 5 | 反馈及时 | ✓ | 每段均有"第 N 段已保存。系统将立即继续监听。"；ASR rtf<0.26、LLM 秒级 |
| 6 | 容错可见 | **✗** | 段 4 降级无"原始记录已保存，结构化暂时不可用"类人话；段 8 结束语识别失败无任何提示；`仍需确认。。` 双句号 |
| 7 | 可理解性 | **✗** | 路径/token/进度条/`目标=权限=采用=缺失字段=`/`最终上下文包含 4 条事件`/双句号"。。" |
| 8 | 语音预算 | 不适用 | TTS 未接入，待 TTS 轮（TTS-01~05）复走 |
| 9 | 响应速度 | ✓ | 识别到反馈间隔短（LLM 处理秒级）；观察点：LLM 处理期屏幕无"处理中"提示 |

### 8.3 发现问题登记（UX_ISSUES，供 PRESENT/TTS/前端逐项对照）

| 编号 | 问题 | 复现 | 期望（对 PRESENT 的要求） |
|---|---|---|---|
| UX-01 | DEBUG 与用户信息同屏混排 | 每段都有 `[LLM请求]`/`[LLM响应]`/token/路径/进度条 | 用户模式只显示对话/回执/状态/待确认列表，DEBUG 走日志 |
| UX-02 | 追问文本埋在开发行内，无"当前问题区" | 段 1 追问在 `[统一链]` 行 | 追问以独立、醒目的"当前问题"呈现（POLICY 当前问题区） |
| UX-03 | 无编号回答易被当实验段 | 段 2"先加入5毫升缓冲液"被记为实验段 | 用户回答无编号时提示"回答请指定问题编号"（已有 GAPS-FIX-ANSWER-HINT-01） |
| UX-04 | 降级/弃权段无用户人话 | 段 4/5 | 显示"原始记录已保存，结构化处理暂时不可用"类提示（POLICY 后台降级示例） |
| UX-05 | 结束语识别失败无提示 | 段 8".别束实验记录。" | 识别到疑似结束意图但未精确命中时，提示"请说'结束实验记录'"（GAPS-FIX-END-01 已计划） |
| UX-06 | 双句号"。。" | 段 6、7 | 统一回执文本拼接，去重标点 |
| UX-07 | "第 N 段"未与"实验步骤 N"分离 | 全程 | 用户界面显示"实验步骤 N"，内部保留 utterance 编号（POLICY 第 5 节） |
| UX-08 | 结束汇总含开发语言 | "最终上下文包含 4 条事件" | 汇总只给用户语言：步骤数/异常数/待确认数（POLICY 第 2 节 B 类） |
| UX-09 | 需要用户注意的事件无声音引导（用户 2026-08-14 提出） | 追问/确认回执/降级/识别失败/结束语未识别时，屏幕信息被调试行淹没，用户需一直盯屏 | 事件触发时播放提示音（复用 `play_wake_tone` 设施，`src/audio/feedback.py` 扩展事件音），用户听到声音再看屏幕；提示音是"注意力信号"，≠ TTS 朗读，两层职责分开 |
| UX-10 | 背景嘈杂时 ASR 误识别增多（用户 2026-08-14 提出） | 2026-08-14 走查会话：'防生缓冲液'（5毫升→防生）、'제가.'、'.别束实验记录.' | 属 ASR 层（非输出层）：新误识别样例已登记进 `evaluation/narration_robustness/narration_plan.json` 段 29/30/31（28→31 段，schema 21 项 + 全量 468 项通过），走既有 ASR 任务线（AUDIO-PREROLL 截音/热词/后处理）；不混入 PRESENT 输出层改动 |
| UX-11 | 终端输出分层：用户版/管理员版（用户 2026-08-14 提出） | 用户模式屏幕只展示 SCREEN 层（对话/回执/状态/待确认列表/指引），DEBUG 层（LLM 请求/响应/token/路径/进度条/状态变化）**单独保留不丢失**；管理员模式屏幕显示全部（现状） | 落地为 `UI_MODE=user|admin` 配置开关：用户模式屏幕只显示 SCREEN 层、DEBUG 写日志文件（如 `results/debug_<session>.log`）；管理员模式屏幕显示全部。需要把 main.py 42 处 print 逐处归类到 SCREEN/DEBUG 层并经统一出口输出。**此需求与 PRESENT-INTEGRATE-01 同源**（消息链路目标），作为其用户可见验收标准，不单独提前实现 |

### 8.4 基线正向确认（PRESENT 不得退化的点）

- 唤醒反馈清晰（"唤醒成功：小科小科"）；
- "查看待确认问题"列表输出友好；
- 每段均有"已保存+继续监听"反馈，响应快；
- 结束指令精确命中时反馈清晰（"检测到结束指令，停止接收新的实验口述。"）；
- 会话开始指引清楚（"说'结束实验记录'可以结束本次会话"）。

### 8.5 走查方法学注意（本次教训）

- 走查期间存在并行实例（`results/walkthrough_baseline_20260814.txt`，
  与用户终端会话同时响应唤醒词），**下次走查只允许一个实例运行**，
  否则两边输出互相干扰、LLM 调用重复。
- 口述者不应边口述边看屏幕（本次用户直接反馈"看不过来"）；
  输出重定向到文件，判断在事后离线进行。

### 8.6 体验状态

`UX_ISSUES`（11 项问题已登记：UX-01~08 基线走查发现，UX-09 提示音引导、UX-10 嘈杂识别、UX-11 用户版/管理员版输出分层为用户 2026-08-14 补充；待输出层/ASR 任务逐项闭环后复走九维表对比）。

### 8.7 RESTORE-NONBLOCK-01 走查（2026-08-15，真实验收会话 20260815_094954）

> 本轮输出面变化：**显示时机**——非阻塞后，每段结果不再"说完立刻出现"，
> 而是"你说下一段时才插空冒出来"（`[LLM响应]` 与 `第 N 段已保存` 交错出现在
> 下一段录音期间）。这是非阻塞的必然权衡：要"不等 LLM、连续说"，结果只能延后显示。

| 维度 | 结论 | 证据（会话 20260815_094954） |
|---|---|---|
| 5 反馈及时 | ✓（延后但可接受） | 每段仍输出"第 N 段已保存。系统将立即继续监听。"，时机从"说完立刻"改为"下一段录音期间插空" |
| 9 响应速度 | ✓ | 非阻塞实锤：每段 `[LLM响应]` 插空出现在下一段录音期间（录音不等待 LLM）；LLM 秒级 |
| 8 语音预算 | 不适用（TTS 未接） | 用户前瞻要求：TTS 阶段不得乱序朗读（登记 `TIMING-02`） |

**体验结论：`UX_CONFIRMED`（用户裁决）**——用户 2026-08-15 明确表态"目前能接受，PRESENT 时展示到界面上影响不大，不要 TTS 乱读就行"。

> 前瞻登记：屏幕"插空显示"的节奏用户已接受；但 TTS 是**线性时间轴**，不得把
> 屏幕乱序照搬成语音乱序——须满足 `TIMING-02`（用户说话时不朗读/不打断）+
> 按顺序朗读。这是本轮体验裁决附带的前瞻约束。

### 8.8 PRESENT END_ONLY 自动走查（2026-08-16）

> 本轮改动了用户可见的结束输出。本节只记自动与代码证据，
> 未使用真实麦克风，因此不宣称 `REAL_OK` 或新的 `UX_CONFIRMED`。

| 维度 | 自动结论 | 证据 |
|---|---|---|
| 1 信息分层 | 改善 | 结束摘要删除“共处理 M 段/提交 N 段”内部统计语言 |
| 2 层次与聚合 | 改善 | 步骤数、待确认数和问题明细合并为一个 `END_ONLY` 块 |
| 4 编号一致 | 保持 | 待确认明细继续使用稳定 `display_number` |
| 6 容错可见 | 改善 | 零待确认不再静默，明确显示“没有待确认问题” |
| 7 可理解性 | 改善 | 收尾改为“本次共记录 N 个实验步骤”的用户语言 |
| 9 响应/收尾 | 待真实复验 | 集成测试证明摘要交付一次；真实慢 sink 与 pump flush 另属 `PRESENT-PUMP-FLUSH-01` |

**自动证据**：专项 41/41、全量 571/571 通过。
**待真实验收**：有/无待确认项的终端多行布局、尾消息是否丢失、用户是否能一眼看懂。

### 8.9 程序级必要反馈自动走查（2026-08-16）

| 维度 | 自动结论 | 证据 |
|---|---|---|
| 1 信息分层 | 改善 | 启动/就绪/唤醒/退出从 logging 恢复为用户反馈，开发详情仍走日志 |
| 2 层次与状态 | 改善 | starting 与 ready 分开，不在模型尚未加载时假称“已就绪” |
| 5 反馈及时 | 自动通过 | pump 在重型模型初始化前启动并先投递 starting |
| 6 容错可见 | 改善 | 启动期间 Ctrl+C 也有明确退出反馈，不只覆盖待机阶段 |
| 7 可理解性 | 改善 | 固定用户文案不含 logger 级别、模型路径或内部状态名 |
| 9 收尾可靠性 | 待 flush 专项 | 快 output 集成测通过；慢 output 的 in-flight 保证由 `PRESENT-PUMP-FLUSH-01` 解决 |

**自动证据**：程序级集成测覆盖正常唤醒后退出与启动期间退出；程序状态/唤醒投影合同有独立测试；专项 56/56、全量 578/578 通过。
**体验状态**：待最终真实 user/admin 对照会话，本轮不代填 `UX_CONFIRMED`。

### 8.10 Pump 完成交付合同自动走查（2026-08-16）

> 本轮没有修改用户文案，但改变了收尾与退出时的交付可靠性；只记录自动证据，
> 未使用真实麦克风，因此不宣称 `REAL_OK` 或新的 `UX_CONFIRMED`。

| 维度 | 自动结论 | 证据 |
|---|---|---|
| 2 顺序与聚合 | 保持 | FIFO、相邻语义去重与 deferred 单问题选择继续由 Coordinator 管理，unfinished 计数不改变呈现顺序 |
| 5 反馈及时 | 改善 | flush 有显式 timeout，不再无限猜测等待；慢 output 测试覆盖未完成状态 |
| 6 容错可见 | 改善 | renderer/output 失败形成结构化交付错误，失败消息不阻断后续消息 |
| 9 收尾可靠性 | 自动通过 | 队列已取空但 output 仍阻塞时 flush 返回 False；释放 output 后才返回 True，证明尾消息不会被“队列空”误判为已交付 |

**自动证据**：专项 24/24、全量 584/584 通过。
**体验状态**：真实终端尾消息与 Ctrl+C 时序并入 `PRESENT-FINAL-UX-VERIFY-01`。

### 8.11 开发输出泄漏自动走查（2026-08-16）

> 本轮改变了 user 终端的输出来源边界，但未启动真实模型和麦克风；以下为自动证据，
> 不宣称 `REAL_OK` 或新的 `UX_CONFIRMED`。

| 维度 | 自动结论 | 证据 |
|---|---|---|
| 1 信息分层 | 改善 | recorder/VAD/wakeword/ASR/state/LLM/main 生产模块直接 print 为 0，内部信息进入 logging |
| 5 反馈及时 | 保持 | 没有用全局 stdout/stderr 重定向，不会在 ASR 并发期间吞掉 Pump 用户消息 |
| 7 可理解性 | 改善 | FunASR 进度条和内部结果表显式关闭，用户信息仍由 PRESENT 文案控制 |
| 9 响应速度 | 保持 | 只关闭依赖显示，不改变模型、录音、识别或业务调度路径 |

**自动证据**：专项 16/16、全量 575/575 通过；AST 护栏防止生产模块重新直接 print。
**待真实验收**：user 模式启动、唤醒、识别和退出全程无模型路径、RTF、音频时长、加载细节和 token。

#### 真实首轮：会话 20260816_141745

| 维度 | 结论 | 证据 |
|---|---|---|
| 1 信息分层 | 未通过，已修待复验 | starting 与 ready 之间出现 FunASR 版本检查、ModelScope 仓库下载日志和进度条 |
| 2 状态指引 | 部分通过，已修待复验 | READY 表达等待唤醒，但没有告诉用户 Ctrl+C 可退出；现已补固定文案 |
| 4 编号一致 | 通过 | 实验步骤1/2、待确认问题1、指定回答问题1一致 |
| 6 容错可见 | 通过 | 自然结束语进入确认，不直接误退出 |
| 9 收尾可靠性 | 通过 | 单一结束摘要完整，退出反馈为最后一条且未丢失 |

**修后自动证据**：专项49/49、全量576/576通过。需最短二次启动验证第三方输出确实消失。

#### 真实二轮：会话 20260816_142352

| 维度 | 结论 | 证据 |
|---|---|---|
| 1 信息分层 | 接近通过，已再修 | 更新/下载日志和进度条消失，仅剩 FunASR 版本横幅；已定位依赖先print后判断disable并精确禁用入口 |
| 2 状态指引 | READY 已通过，WAITING 待复验 | READY 已含 Ctrl+C；会话后此前没有再次唤醒说明，现新增 WAITING 状态 |
| 4/6/9 | 通过 | 两步编号、追问/回答、自然结束确认、结束摘要和退出反馈均正确 |

**修后自动证据**：专项63/63、全量576/576通过。下次只需确认零版本行、WAITING 可见和第二次唤醒成功。

#### 真实三轮：会话 20260816_142945

| 维度 | 结论 | 证据 |
|---|---|---|
| 1 信息分层 | ✓ | starting→ready 及整轮无 FunASR/ModelScope、路径、进度条、RTF、耗时或 token 泄漏 |
| 2 状态指引 | ✓ | READY 说明 Ctrl+C；结束摘要后 WAITING 说明再次唤醒；Ctrl+C 后 EXITED |
| 4 编号一致 | ✓ | 实验步骤1/2、待确认问题1和回答问题1一致 |
| 6 容错可见 | ✓ | 查看、问题回答、精确结束命令均有明确反馈 |
| 9 收尾可靠性 | ✓ | 摘要→WAITING→EXITED 顺序完整，无尾消息丢失 |

**体验结论**：`PRESENT-FIX-LEAK-01 = REAL_OK`。本轮未实际第二次唤醒，双会话仍并入最终 PRESENT UX 验收。

#### 双会话复验：20260816_143151 → 20260816_143201

| 维度 | 结论 | 证据 |
|---|---|---|
| 1 信息分层 | ✓ | 两轮全程零第三方和开发输出泄漏 |
| 2 状态与层次 | ✓ | 第一轮摘要→WAITING→二次WAKE_ACK→新会话；第二轮摘要→WAITING→EXITED |
| 4 编号一致 | 编号通过，复合回答执行失败 | 问题1–4稳定；问题2的“是的+体积50毫升”被确定性规则只执行CONFIRM，体积未填入，并非LLM漏提取 |
| 6 容错可见 | ✗，已归入下一项 | “制业枪”只有ASR回显，无业务处理反馈，再次证明no_action沉默 |
| 9 生命周期/收尾 | ✓ | 同进程两轮会话、两个session_id、两次完整摘要，最终退出无尾消息丢失 |

**结论**：再次唤醒真实通过；FIX-LEAK继续为REAL_OK。下一体验修复为NOACTION；另登记P0复合确认+回答执行缺陷。
